apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx.fullname" . }}
spec:
  selector:
    app: {{ include "nginx.name" . }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: 80

