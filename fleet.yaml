defaultNamespace: demo

helm:
  chart: ./nginx-helm
  releaseName: nginx
  valuesFiles:
    - ./nginx-helm/values.yaml

targets:
- name: all
  clusterSelector:
    matchExpressions:
    - key: fleet.cattle.io/cluster-name
      operator: Exists
